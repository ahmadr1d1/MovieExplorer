<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="110dp"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="8dp"
    tools:ignore="TextSizeCheck">

    <!-- Shapable ImageView -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/image_cast"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:contentDescription="@string/cast_member_image_content_description"
        android:scaleType="centerCrop"
        android:src="@drawable/baseline_person_24"
        app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Full" />

    <!-- Cast Name -->
    <TextView
        android:id="@+id/tv_cast_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:ellipsize="end"
        android:maxWidth="100dp"
        android:maxLines="2"
        android:text="@string/cast_member_name_placeholder"
        android:textAlignment="center"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:textStyle="bold" />

    <!-- Cast Role -->
    <TextView
        android:id="@+id/tv_cast_character"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:ellipsize="middle"
        android:maxWidth="85dp"
        android:maxLines="2"
        android:text="@string/cast_member_character_placeholder"
        android:textAlignment="center"
        android:textColor="@color/text_secondary"
        android:textSize="12sp" />

</LinearLayout>
